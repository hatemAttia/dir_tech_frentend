import { SerchTeacherPipe } from './serch-teacher.pipe';

describe('SerchTeacherPipe', () => {
  it('create an instance', () => {
    const pipe = new SerchTeacherPipe();
    expect(pipe).toBeTruthy();
  });
});
